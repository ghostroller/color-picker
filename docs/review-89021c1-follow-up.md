# 89021c1 审查复核与修复

复核日期：2026-09-22。工作树起点为 `89021c1c093dddcbce65b014e28bed861835426b`。
审查材料为 `color-picker-review-89021c1.md`；本记录以实际源码和执行结果为准。
材料中的提交拆分及“交给后续编程助手”的文字是建议，不作为额外执行指令。

## 逐项结论

| 编号 | 复核结论 | 处理 |
|---|---|---|
| CP-01 | 成立。构造及缩放通过 `hit_test` 更新 hover，绘制却读取 `selected`，窗口/视图裁切后确实可以指向不同像素。 | 高亮、色块、文本统一从 hover 派生；保留缩放锚点和点击事件的实时命中计算。 |
| CP-02 | 成立。结果窗只约束位置；设置窗约束外框但保留固定横向内容布局。 | 共用工作区尺寸约束，结果内容可滚动且保留操作区，设置窄屏重排。 |
| CP-03 | 成立。Live 会消费并丢弃移动邮箱，实际采样仍来自计时器与当前光标。 | Live 禁用移动通知，Frozen 启用；切换清理旧 pending，保留独立输入线程和会话隔离。收益百分比未测。 |
| CP-04 | 成立。原 quick pick 先创建完整结果 UI，再隐藏复制。 | 宿主持有独立 CopyJob，成功不创建结果窗，失败才显示可手动恢复的结果；保留有界重试与失效 token。 |
| CP-05 | 成立，属于原保护范围之外的编辑冲突。原二次比较只能覆盖保存进行期间。 | 运行时保留加载时的原始字节 revision；保存前检查，成功更新 revision，冲突保留草稿并提示重启加载。 |
| CP-06 | 成立，属于证据缺口。构造/消息测试不能代替完整真实交互，历史性能数字也不能代表本次构建。 | 增补与修复对应的自动回归；完整交互及动态性能仍须在独立 Windows VM/测试账户执行，见下表。 |

没有发现需要推翻的核心技术结论。需要保留报告原有的限定：无效通知不等于已发生
计时器饥饿；构造了隐藏结果窗不等于已抢焦点；更高工作集不等于泄漏；配置比较加
rename 不是对任意外部写入者都成立的原子 CAS。

Win32 语义亦与原始文档一致：[WM_TIMER](https://learn.microsoft.com/en-us/windows/win32/winmsg/wm-timer)
是低优先级消息；[LowLevelMouseProc](https://learn.microsoft.com/en-us/windows/win32/winmsg/lowlevelmouseproc)
需要安装线程的消息循环并要求回调迅速返回；[OpenClipboard](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-openclipboard)
的 NULL owner 不能替代快速复制需要的有效 HWND。

## 本轮验证

环境：Windows x64，系统版本 `10.0.19045.0`，Rust/Cargo 1.95.0，静态 CRT。
本轮基于上述提交加本工作树修改，尚未产生新提交或发布包。

| 检查 | 结果 | 范围 |
|---|---|---|
| `scripts/verify-windows.ps1` | PASS | 格式、全目标 Clippy（warnings 为错误）、122 项默认测试、x64 Release、EXE 实际 manifest、主程序及像素示例 PMv2。 |
| 结果窗显式冒烟 | PASS，8 项 | 窄窗口、焦点滚入、错误提示、失败后手动恢复、复制反馈及旧 timer；复制结果通过注入模拟，不写真实剪贴板。 |
| `windows_selection_ui` | PASS，1 项 | 原生放大镜/结果/设置控件；包含窄设置窗控件边界断言，无输入注入。 |
| 设置保存事务 | PASS，1 项 | 临时目录和自有 HWND，临时注册空闲的 Ctrl+Alt+Shift+F9/F10/F11；覆盖外部有效修改冲突时旧配置/revision/快捷键保留及临时键释放。 |
| `git diff --check` | PASS | 无空白错误。 |

`windows_selection_ui` 首次在沙箱中读取光标被拒绝（`0x80070005`），
随后在交互桌面执行通过。上述测试清理自己的窗口与临时资源，不修改用户配置、
不合成键鼠输入、不写真实剪贴板。当前桌面已有的用户 `color-picker` 进程未被重启或接管，
其性能计数未混入本轮结果。

最终验证用 Release EXE：`target/x86_64-pc-windows-msvc/release/color-picker.exe`，
SHA256：`A5CF5E9B69AB52D2C40A789DE792154328A62E38A530BF6A47CF32EF3C168079`。
构建/manifest 验证日志保存在本地 `logs/review-89021c1-verify.txt`。

本轮还修复了复核中发现的两个关联问题：窄设置预览的底图与文字/边框同步居中裁剪，
初始结果与设置窗口使用已选显示器的工作区，避免展开矩形改选邻屏后 DPI 与尺寸不一致。
设置窗口最小化时跳过零客户区的重排。

启用 `--log-file` 后会话结束新增 `session.counts`，记录移动事件、移动唤醒和采样次数；
输入钩子仅执行可选的原子计数，不在回调中格式化或写文件。
计数器与逻辑测试为后续测量提供依据，不代表已取得动态路径的性能结果。

## 尚需独立桌面的验收

以下项目均为 **NOT TESTED**，不会因自动测试通过而自动改成 PASS。
仅在一次性 Windows VM 或专用测试账户、无用户数据的剪贴板与配置中执行。

| 场景 | 操作与需要记录的断言 |
|---|---|
| 完整流程 | 实际热键 → Live → 点击；实际热键 → 滚轮 Frozen → 点击；输入释放完成后颜色/坐标正确、真实剪贴板文本正确，再次激活成功。 |
| Quick pick | 成功时没有结果窗/子控件、前台不变、重试计时器为零；Busy 后释放成功，持续 Busy 或其他错误才显示结果，随后手动复制成功不自动关闭。 |
| 旧复制任务 | Busy 重试期间重新激活/退出；延迟到达的旧 timer 不写新任务的剪贴板，不显示旧结果。 |
| 冻结边缘 | 四角、任务栏、自定义边框、负坐标副屏；4→8→16→32 后移向图像边缘再缩回；高亮、文字、同点点击保持一致，移出内容后无旧高亮。 |
| 小工作区 | 900×650 和 800×650 物理像素、192 DPI，中英文与复制失败提示；窗口/操作区位于工作区，滚动和 Tab 可到达全部内容。跨 DPI/改变工作区后重验。 |
| 取消/环境 | Esc、右键、窗外点击、按住再取消，锁屏恢复、显示变化、Explorer 重启；无残留钩子，下一次取色成功。 |
| 重复使用 | 固定预热后，包含 Live/Frozen/结果/复制/取消的混合循环；记录句柄、GDI、USER、线程、工作集与私有提交趋势。 |

动态性能沿用 `scripts/measure-passive-app.ps1`、`docs/measurements/README.md`
及现有报告格式。在同一机器、同一构建类型、同一鼠标轨迹下分别采集基线与本次构建：
Live/Frozen、静止/持续移动、透明度 0%/35%。记录提交及工作树状态、EXE SHA256、
OS/CPU/DPI、鼠标回报率、CPU 时间、移动/唤醒/采样/重绘计数、采样间隔 p50/p95/p99
和激活延迟。不可把 17ms 请求间隔写成实测 60 FPS，也不可从每秒采样次数推导
逐帧间隔分位数。未采集的数据留空并标 NOT TESTED，不填零或估计的收益。

CP-05 的回归使用独占临时目录：覆盖 A 加载 → 外部 B → 草稿 C 保存拒绝、外部
创建/删除、只改 JSON 空白、损坏配置保护、重新加载后保存及连续应用更新 revision。
冲突时仍保留原快捷键事务与 UI 草稿。该保护不支持自动合并，也不消除最后一次
比较与 rename 之间的竞态窗口。
